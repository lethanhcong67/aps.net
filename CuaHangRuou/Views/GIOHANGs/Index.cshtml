@model IEnumerable<CuaHangRuou.Models.GIOHANG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KHACHHANG.HoTen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KHACHHANG.HoTen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaGH }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaGH }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaGH })
        </td>
    </tr>
}

</table>
